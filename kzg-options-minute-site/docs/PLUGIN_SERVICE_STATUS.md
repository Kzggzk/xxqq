# Plugin and service status / 插件与服务接入状态

This is the operational ledger for every external plugin or service touched, planned, or explicitly held back for KZG Option House. It is internal documentation for Fangbao and future authorized agents. Do not publish this table on the public website.

这是 KZG Option House 的外部插件与服务总账，记录已经使用、正在规划、或明确暂停的服务。它是给 Fangbao 和后续授权 agent 的内部文档，不得放到公开网站。

## Service ledger / 服务总账

| Service / Plugin | Status | What has actually happened / 已实际完成 | Boundary and next action / 边界与下一步 |
|---|---|---|---|
| GitHub | Active | Source and docs are pushed on branch `feat/kzg-option-house-daily-auto`. v1.71 is the latest correction: public homepage internal/planning copy is scrubbed, the visible flow tape blur gate is removed, and commercial infrastructure planning moved into internal docs. Production is now v1.71. iOS companion remains `0.7`. | Keep committing source/docs only. Do not commit raw generated data under `public/data/`, `public/reports/`, `.private/`, `dist/`, or raw `.csv.gz`. |
| Netlify | Active | Production site is `https://kzg-option-house.netlify.app/`. Latest production deploy is v1.71 at `https://6a16bc992cdd920079852497--kzg-option-house.netlify.app/`, deploy id `6a16bc992cdd920079852497`. Production `/data/index.json` and `/assets/kzg-pack.js` both verify as 404; `/app.js` verifies `UI_VERSION="1.71"`; production Browser/Playwright desktop and phone checks passed with public-copy scan false, overflow 0, blur/lock/gate/veil 0, console issue 0, and PNG export `1,482,138` bytes. | Production raw data endpoint `/data/index.json` should remain 404 unless Fangbao explicitly changes policy. |
| Apple Notes | Active | Pinned owner note title is `CHANGLOG 期权终端`. It is synced from `docs/CHANGLOG_OPTION_TERMINAL.md`; latest v1.71 sync returned `updated=1`, `created=0`, `223303` body characters and records the public-copy scrub plus stopped heartbeat. | The note must be a real operating ledger, not a short status. This service matrix must be summarized into the note on each docs update. |
| Massive | Active as data source and research topic | Flat-file data currently powers the site. Local proof is `505` option-minute files covering `2024-05-17 -> 2026-05-22`. v1.61 rechecked the public Options Minute Aggregates flat-file docs at <https://massive.com/docs/flat-files/options/minute-aggregates?assetClass=options&display=all&license=personal>: `2023/2023` exists publicly, but download entitlement still depends on account/history permission. Official Massive pricing/docs were rechecked for v1.51 real-time product planning: Options Advanced has real-time data, WebSockets, snapshots, trades, quotes, Greeks/IV, OI, and flat files; WebSocket quotes are capped at 1,000 contracts per connection; default WebSocket connection policy is one concurrent connection per asset class. Fangbao's dashboard screenshot exposed real API keys in the conversation context, but this agent did not use, copy, store, or commit them. | Rotate/regenerate exposed keys before any real backend integration. No API key is used in browser, iOS, docs, or Git. No plan upgrade has been executed. Real-time paid SaaS needs backend ingestion plus Business/OPRA display and redistribution entitlement confirmation before public sale. Re-audit 2023 with a safe credential channel only. |
| iCloud Drive | Active as local raw-data storage | Local path verified earlier: `/Users/fangbao/Library/Mobile Documents/com~apple~CloudDocs/KZG/23_DATA_Massive_期权分钟_Minute`. Current local proof is 505 files, `2024-05-17 -> 2026-05-22`. | Treat `2023-05` as a target to re-audit, not as landed local proof. Keep local raw files out of Git. |
| Google Drive | Reported mirror, not connector-verified this turn | Fangbao reported Google Drive has the same 505 files and matching size. | Future agent with Google Drive connector should verify file count, byte size, and range directly, then write evidence here. |
| Browser / Playwright / Chrome | Active for QA | v1.60 used Browser first on `http://127.0.0.1:4192/`, then Playwright for local dist `http://127.0.0.1:4193/` and live production. v1.61 used Browser first on `http://127.0.0.1:4194/`, then Playwright on local dist `http://127.0.0.1:4195/`. v1.62 used Browser on `http://127.0.0.1:4198/` and Playwright on dist `http://127.0.0.1:4199/`. v1.63 used Browser first on `http://127.0.0.1:4200/`, then Playwright on dist `http://127.0.0.1:4201/` and production. v1.64 used Browser on `http://127.0.0.1:4202/`, then Playwright on dist `http://127.0.0.1:4203/`. v1.65 used Browser on `http://127.0.0.1:4204/`, then Playwright on dist `http://127.0.0.1:4205/`. v1.66 used Playwright on dist `http://127.0.0.1:4207/` after the Browser agent was not exposed. v1.68 again used Playwright because the Browser local-control tool was not exposed in the current tool set; local dist `http://127.0.0.1:4208/` and production both passed. v1.69 used Playwright on local dist `http://127.0.0.1:4209/` after Browser local-control was not exposed. v1.70 used Browser plugin first on `http://127.0.0.1:4210/`, then Playwright for desktop/phone/PNG proof: `1.70 · 505/505 complete`, momentum compass 1, steps 3, jump 1, focus `RGTI`, RGTI queue row 1, active RGTI row 1, rotation focus 1 after compass jump, horizontal overflow 0, old `.is-blurred`/`.is-locked` 0, future gate 1, console issue 0, public provider/API/payment text false, and PNG export `1,482,138` bytes. v1.70 screenshots: `/tmp/kzg-option-house-v170-dist-desktop.png`, `/tmp/kzg-option-house-v170-compass-desktop.png`, `/tmp/kzg-option-house-v170-dist-mobile.png`, `/tmp/kzg-option-house-v170-compass-mobile.png`, `/tmp/kzg-option-house-v170-export.png`. | Continue using Browser first when operational, then Playwright for responsive/PNG proof. Screenshots are QA evidence, not committed artifacts unless explicitly requested. |
| iOS / Xcode | Active companion scaffold | Native SwiftUI app created at `/Users/fangbao/kzg-options-minute-site/ios/KZGOptionHouse/KZGOptionHouse.xcodeproj`, scheme `KZG Option House`, bundle `com.kzg.optionhouse`; current iOS `0.7` source typecheck passes. XcodeBuildMCP sees the right profile but `build_run_sim` is blocked locally because the configured simulator destination cannot be found: `{ platform:iOS Simulator, id:9DAFEA29-80F2-4D94-BE75-C0106CE8D97E }`. | iOS updates every 5 Web dense versions. Next default sync is around Web `1.72`. App Store signing, TestFlight upload, developer-account changes, or review submission require Fangbao confirmation. |
| Stripe | Planned only | Payment architecture is documented, but no Stripe product, Checkout Session, Customer Portal, webhook, or real price was created. | Stop before creating products, changing Stripe account state, transmitting secrets, or collecting money. Needs Fangbao approval. |
| Supabase | Planned only | Candidate auth/schema/entitlement architecture is now documented in `docs/COMMERCIAL_INFRASTRUCTURE_PLAN.md`: profiles, subscriptions, entitlements, usage events, export jobs, signal snapshots, RLS, backend-only service role, and auth callback functions. No project connection or table mutation has happened. | Needs Fangbao approval and credentials/project selection before real backend work. Keep public page free of Supabase internals. |
| Network Solutions / domain registrar | Blocked before spend | Domain purchase path is now documented in `docs/COMMERCIAL_INFRASTRUCTURE_PLAN.md`: availability, first-year price, renewal price, privacy, DNS, SSL, redirect, and rollback checklist. No domain was bought and no DNS was changed. | Stop before any purchase, renewal, checkout, or DNS change. Ask Fangbao at the exact spending step. |
| WeChat Pay route | Planned only | Treated as China-money payment rail in internal docs. No merchant credential or QR collection flow has been connected. | Requires Fangbao approval and likely merchant account details. Do not put implementation details on public site. |
| USDT / wallet route | Planned only | Treated as later crypto payment rail. No wallet login, signature verification, address mapping, or chain collection has been connected. | Needs product decision and security design. Wallet login is not the same as payment. |
| Massive real-time backend | Planned only | v1.55 turns that boundary into public UX: top daily dashboard open, middle future realtime-flow reserve, bottom historical intraday layer open. v1.56 refines the future realtime area into a flow-book/filter-tree prototype. v1.57 mirrors that boundary in iOS `0.5`. v1.58 adds a Web transition rail and filter-weight rail. v1.59 adds the top-level three-sector product spine and removes the provider name from the public displayed dataset label. v1.60 compresses the middle reserve into a Flow Router with bias, premium, strategy, and risk gates. v1.61 compresses that reserve on phones. v1.62 adds the open-layer handoff from future realtime reserve into the free 505-day history layer and mirrors Flow Router in iOS `0.6`. v1.63 turns that open layer into a four-step path router. v1.64 adds a public-open four-beat story strip for drift, pressure minute, breadth, and premium-notional anchor. v1.65 adds story-to-panel focus feedback so open-history clicks land visibly in the target panel. v1.66 adds a bucket-to-rotation bridge inside the open historical layer, connecting pressure bucket, CP extremes, and breadth before jumping into symbol rotation. v1.67 mirrors that bridge in iOS `0.7`. v1.68 adds a rotation-to-symbol focus bridge and deploys it to production. v1.69 adds a symbol-to-momentum queue bridge and keeps the focused RGTI row visible in the open queue. v1.70 adds the momentum queue compass for capital anchor, CP edge, and active row. This is still a UI/schema prototype, not a real backend connection. | Next backend-only planning can model fanout, cache, event compression, and entitlement, but do not connect real keys or sell real-time OPRA-derived feed until business/legal entitlement is confirmed. |
| Automation heartbeat | Deleted by Fangbao request | `kzg-option-house-pro-evolution-loop` was deleted on 2026-05-27 after Fangbao told Codex to stop automatic updating immediately. It should no longer wake up every 15 minutes. | Do not recreate or replace this automation unless Fangbao explicitly asks. |
| Canva / Figma / Slack / Gmail / Calendar / Notion | Not used for this product phase | Available in environment, but not used for current code, deploy, payment, or data workflow. | Only use when Fangbao explicitly asks for design artifacts, messaging, calendar, or knowledge-base publishing. |

## Practical answer / 直接回答

中文:

已经跑通的是 GitHub、Netlify、Apple Notes、本地 iCloud 数据路径、浏览器/Playwright 验证、PNG 导出，以及原生 SwiftUI iOS 伴生工程到 `0.7` 的源码验证。v1.71 是当前生产版本：公开页已清理内部产品/规划话术，中段改为普通期权流样张，公开 DOM 中 blur/lock/gate/veil 均为 0；Network Solutions、登录/auth、Supabase 和权益路线只在 `docs/COMMERCIAL_INFRASTRUCTURE_PLAN.md`。已经研究但还不能上线的是 Massive 实时 API 路线；2023 公开目录存在但账号下载权限要安全复核。只写了架构、没有真实接入的是 Stripe、Supabase、Network Solutions 或其他域名商、微信支付、USDT/钱包。Google Drive 的 505 文件镜像来自 Fangbao 回执，未来需要 connector 直接复核。Canva、Figma、Slack、Gmail、Calendar、Notion 当前没用，因为这阶段核心不是设计稿或消息系统，而是数据终端、部署、交接、iOS 伴生端和未来 SaaS 架构。

English:

The working layer is GitHub, Netlify, Apple Notes, local iCloud data, browser/Playwright QA, PNG export, and native SwiftUI iOS companion source verification up to `0.7`. v1.71 is the current production version: the public page has been scrubbed of internal product/planning copy, the middle section is plain options-flow sample data, and the production DOM has blur/lock/gate/veil all at 0. Network Solutions, auth/login, Supabase, and entitlement planning now live only in `docs/COMMERCIAL_INFRASTRUCTURE_PLAN.md`. Massive real-time API has been researched but is not ready for public resale; the 2023 public directory exists, but account download entitlement needs safe re-audit. Stripe, Supabase, Network Solutions or any domain registrar, WeChat Pay, and USDT/wallet are architecture-only and not live. Google Drive's 505-file mirror is Fangbao-reported and should be connector-verified later. Canva, Figma, Slack, Gmail, Calendar, and Notion are unused because this phase is about the data terminal, deployment, handoff, and future SaaS architecture.
